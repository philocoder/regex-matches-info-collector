This script contains a function that collects the position, the match and the groups of each RegExp match in a text.

Nothing else. 

Because JavaScript didn't put things easy to do this, I provided this function.

